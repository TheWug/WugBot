#include "rsp.h"
#include "IRCBot.h"

void rsp::Register(IRCBot& bot)
{
	bot.SetResponse(RespondTo(), this);
}

void rsp::PostAction(IRCBot& bot, Hostname& speaker, string& command, string target, string message, bool ctcpflag)
{
}
